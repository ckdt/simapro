<!doctype html>
<!--[if lt IE 7]><html class="no-js ie ie6 lt-ie9 lt-ie8 lt-ie7" {{site.language_attributes}}> <![endif]-->
<!--[if IE 7]><html class="no-js ie ie7 lt-ie9 lt-ie8" {{site.language_attributes}}> <![endif]-->
<!--[if IE 8]><html class="no-js ie ie8 lt-ie9" {{site.language_attributes}}> <![endif]-->
<!--[if gt IE 8]><!--><html class="no-js" {{site.language_attributes}}> <!--<![endif]-->
    <head>
    <meta charset="{{site.charset}}" />
    <title>{{wp_title}}</title>
    <meta name="description" content="{{site.description}}">
    <link rel="stylesheet" href="{{site.theme.link}}/style.css" type="text/css" media="screen" />
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="pingback" href="{{site.pingback_url}}" />

    {% if canon %}
    <!-- Canonical (custom) -->
    <link rel="canonical" href="{{canon}}" />
    {% endif %}

    {% if ga %}
    <!-- Google Analytics (custom) -->
    {{ga}}
    {% endif %}

    {{function('wp_head')}}

    <!-- HTML5 shim and Respond.js IE8 support of HTML5 elements and media queries -->
    <!--[if lt IE 9]>
    <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
    <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->

    {% block head %} {% endblock %}
    </head>

<?php if ( is_user_logged_in() ) { ?>
  <style  type="text/css" media="screen">
    .navbar { margin-top: 30px; }
    #langpanel{ margin-top: 30px;}
    /*#contact{background:#fff url('img/background.jpg') no-repeat 0 280px;}*/
  </style>
<?php } ?>

  <body class="{{body_class}}" data-template="base.twig" data-spy="scroll" data-target=".navbar" data-offset="80">
      <div id="langpanel" class="hidden-xs">
  		<div class="container">
  			<div class="row">
  				<div class="col-sm-12">
  					<h2 class="text-center">Select your country</h2>
  				</div>
  				<div class="col-sm-3">
  					<h4>Europe</h4>
              <ul>
                {% for item in menu_eu.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="eu-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
                {% endfor %}
              </ul>
  				</div>
  				<div class="col-sm-3">
  					<h4>America</h4>
              <ul>
                {% for item in menu_usa.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="usa-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
                {% endfor %}
              </ul>
            <h4>Australia</h4>
              <ul>
                {% for item in menu_aus.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="aus-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
                {% endfor %}
              </ul>
            <h4>Africa</h4>
            <ul>
              {% for item in menu_afr.get_items %}
                <li><a href="{{item.url}}" class="btn-head" data-track-name="afr-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
              {% endfor %}
            </ul>
  				</div>
  				<div class="col-sm-3">
  					<h4>Asia</h4>
              <ul>
                {% for item in menu_asi.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="asi-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
                {% endfor %}
              </ul>
            <h4>Middle East</h4>
              <ul>
                {% for item in menu_mid.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="mid-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}}</a></li>
                {% endfor %}
              </ul>
  				</div>
  				<div class="col-sm-3">
  					<h4>Didn't find your country?</h4>
              <ul>
                {% for item in menu_other.get_items %}
                  <li><a href="{{item.url}}" class="btn-head" data-track-name="oth-{{item.post_title}}" data-track-url="{{item.url}}">{{item.post_title}} {#{{item.subtitle}}#}</a></li>
                {% endfor %}
              </ul>
  				</div>
  			</div>
  		</div>
  	</div>

    <div id="home" class="site">
      <div class="navbar navbar-default navbar-fixed-top" role="navigation">
			<div class="container">
				<div class="navbar-header">
					<button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
						<span class="sr-only">Toggle navigation</span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
						<span class="icon-bar"></span>
					</button>
					<a href="#home" class="logo" data-track-name="home" data-track-url="{{site.url}}/#home"><img src="{{theme.link}}/img/simaprologo.svg" alt="home" width="170" height="34"></a>
				</div>
				<div class="collapse navbar-collapse">
					<ul class="nav navbar-nav">
						<li class="mobile-hidden"><a href="#intro" class="hidden">Home</a></li>
						<li><a href="#usp" class="btn-head" data-track-name="Features" data-track-url="{{site.url}}/#usp">Features</a></li>
						<li><a href="#about" class="btn-head" data-track-name="About SimaPro" data-track-url="{{site.url}}/#about">About SimaPro</a></li>
						<li><a href="#contact" class="btn-head" data-track-name="Contact" data-track-url="{{site.url}}/#contact">Contact</a></li>
					</ul>
					<ul class="nav navbar-nav pull-right hidden-xs">
						<li><a href="{{trynow.get_field('button_url')}}" class="tryout hidden-sm mobile-hidden btn-head" data-track-name="{{trynow.get_field('button_label')}}" data-track-url="{{trynow.get_field('button_url')}}"><strong>{{trynow.get_field('button_label')}}</strong></a>
					    <li class="language"><a href="#" class="language">{{lang_name}}</a></li>
						<li><div class="arrow-down"></div><div id="panell" class="country flag flag-{{lang_class}}"></div></li>
                    </ul>
				</div><!--/.nav-collapse -->
			</div>
		</div>
